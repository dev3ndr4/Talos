# --- LLM CONFIGURATION ---
# The API key for Google AI Studio (Gemini)
GEMINI_API_KEY=your_gemini_api_key_here
# The model to use (e.g., gemini/gemma-4-31b-it, gemini/gemini-1.5-pro)
LLM_MODEL=gemini/gemma-4-31b-it

# --- DATABASE CONFIGURATION ---
# Provide your external MongoDB connection string here
MONGODB_URL=mongodb+srv://user:pass@cluster.mongodb.net/talos_db?retryWrites=true&w=majority

# --- SECURITY ---
# Secret key for JWT generation (change this to a random string)
SECRET_KEY=change-me-to-a-random-string-for-production

# --- FRONTEND CONFIGURATION ---
# IMPORTANT: VITE_API_URL must point to the BACKEND_PORT below if accessing from a browser
VITE_API_URL=http://localhost:8000/api
# The port exposed on the host for the backend
BACKEND_PORT=8000
# The port exposed on the host for the frontend
FRONTEND_PORT=3000
